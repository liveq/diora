{
  "name": "diora-chat-functions",
  "version": "1.0.0",
  "description": "Firebase Functions for DIORA Chat",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.7.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "@types/node": "^18.15.0"
  },
  "engines": {
    "node": "18"
  }
}