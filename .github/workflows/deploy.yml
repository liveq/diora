name: Deploy React App to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build
      run: npm run build
      env:
        CI: false
        REACT_APP_FIREBASE_API_KEY: AIzaSyBACitk-0T_mN1FZ3XgyMgxD-Nv1To4xE0
        REACT_APP_FIREBASE_AUTH_DOMAIN: diora-60d78.firebaseapp.com
        REACT_APP_FIREBASE_DATABASE_URL: https://diora-60d78-default-rtdb.firebaseio.com
        REACT_APP_FIREBASE_PROJECT_ID: diora-60d78
        REACT_APP_FIREBASE_STORAGE_BUCKET: diora-60d78.firebasestorage.app
        REACT_APP_FIREBASE_MESSAGING_SENDER_ID: "327432452340"
        REACT_APP_FIREBASE_APP_ID: "1:327432452340:web:dfc5e328912fcfc45806a1"
        REACT_APP_FIREBASE_MEASUREMENT_ID: G-B28RH1TKEH
        REACT_APP_TELEGRAM_BOT_TOKEN: "8414386876:AAEVuvEtjz_bW7ySbE9HPpjnazODNR6YRG0"
        REACT_APP_TELEGRAM_CHAT_ID: "5969399666"

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build
        publish_branch: gh-pages
        force_orphan: true